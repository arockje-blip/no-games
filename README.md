# Split Workspace

- `extension/lap` is the Chrome/Edge browser extension.
- `lap` is the Windows laptop-wide blocker and startup app.

The two are intentionally separate so browser-only blocking and whole-device blocking do not conflict.
